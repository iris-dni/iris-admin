export const anonymizeTelephone = (phone, visibleCharacters, replaceWith) => {
  if (typeof(phone) === 'string') {
    return phone.substring(0, visibleCharacters) + phone.substring(visibleCharacters).replace(/[0-9]/g, replaceWith);
  }
  return phone;
};
