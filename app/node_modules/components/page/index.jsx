import * as React from 'react';
import SidebarToggler from 'components/sidebar-toggler';


export default (Component) => React.createClass({
  displayName: `Stateful${Component.displayName || Component.name || 'Container'}`,

  render() {
    return (
      <div className="page">
        <div style={{ position: 'absolute', left: 0, right: 0, backgroundColor: 'rgb(244, 249, 253)', top: 0, height: 40 }}>
          <div style={{ position: 'absolute', marginLeft: 40, marginRight: 40, left: 0, top: 0, right: 0, bottom: 0, display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
            <div style={{ maxWidth: '100%', whiteSpace: 'nowrap', textOverflow: 'ellipsis', overflow: 'hidden' }}>Iris</div>
          </div>
          <SidebarToggler />
        </div>
        <div style={{ padding: '15px 0', position: 'absolute', left: 0, right: 0, top: 40, bottom: 0, overflow: 'auto' }}>
          <Component {...this.props} />
        </div>
      </div>
    );
  }
});
