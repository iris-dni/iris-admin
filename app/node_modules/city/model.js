/**
 * The model of a city.
 */
const City = (obj) => {
  if (typeof obj !== typeof {} || obj === null) {
    return null;
  }
  return {
    id: obj.id,
    state: obj.state,
    name: obj.name,
    provider: obj.provider,
    zips: obj.zips,
    tags: obj.tags,
  };
};

export default City;
